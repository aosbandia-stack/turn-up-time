# Agent compatibility instructions

The canonical workflow is defined in `CLAUDE.md` and the skills under `.claude/skills/`.

Do not apply generic TDD, fixed coverage, plan-first, or default fan-out rules unless an approved
project ticket requires them. Route software work through `/turn-up-time`, use the smallest valid
process, and preserve the separation of duties defined in `CLAUDE.md`.
